# 面接日程調整アプリ 要件定義書

**文書番号**: REQ-2026-001  
**作成日**: 2026-05-05  
**版数**: 1.1  
**対象読者**: 開発者・人事担当者  

---

## 1. システム概要

### 1.1 目的

本システムは、人事担当者が行う採用選考に関わる業務（候補者管理・面接日程調整）をブラウザ上で完結させるWebアプリケーションである。

主な目的は以下のとおり。

- **業務効率化**: 手作業・手入力を最小化し、マウス操作中心の直感的なUIで作業時間を短縮する
- **事務負担軽減**: 繰り返し入力が必要なマスタ情報（面接官・大学・職種等）を事前登録してワンクリックで呼び出せるようにする
- **事務処理ミスの削減**: 自動入力・バリデーション・一覧表示により転記ミス・入力漏れを防ぐ
- **データの利活用**: IndexedDBへの蓄積データをCSVエクスポートし、外部ツールでの分析・共有を可能にする

### 1.2 利用者

| 役割 | 説明 |
|------|------|
| 人事担当者 | 本システムの主たる利用者。候補者情報の管理と面接日程の調整を行う |

### 1.3 動作環境

| 項目 | 内容 |
|------|------|
| 動作環境 | モダンブラウザ（Chrome / Edge / Firefox 最新版） |
| ネットワーク | **完全ローカル動作（インターネット接続不要）** |
| サーバー通信 | **なし** |
| データベース | **なし（IndexedDBを利用）** |
| 配布形式 | 静的HTML/CSS/JS（単一ディレクトリで完結） |

---

## 2. 機能要件

### 2.1 共通機能

#### 2.1.1 マスタ管理

手入力を削減するため、以下のマスタを事前登録できる。登録後はフォーム上でワンクリックで選択可能とする。

| マスタ名 | 管理項目 |
|----------|----------|
| 面接官マスタ | 氏名、本部、部署、役職 |
| 大学マスタ | 大学名、区分（国公立 / 私立） |
| 職種マスタ | 職種名 |
| 勤務地マスタ | 拠点名、都道府県 |
| 担当者マスタ | 担当者名（人事内の担当者） |
| 面接場所マスタ | 場所名（会議室名など） |

#### 2.1.2 CSVインポート / エクスポート

- 候補者情報・面接情報ともにCSV形式でエクスポート可能
- 候補者情報はCSV形式でインポート可能（ヘッダー行あり、文字コードUTF-8）
- インポート時は既存データとの重複チェックを行い、警告を表示する
- エクスポートファイル名には日時を含める（例: `candidates_20260505_1200.csv`）

#### 2.1.3 データ永続化

- 全データはブラウザのIndexedDBに保存する
- ページリロード後もデータは保持される
- データ全削除機能（リセット）を設け、実行前に確認ダイアログを表示する

---

### 2.2 候補者管理機能

#### 2.2.1 概要

候補者の登録・編集・削除・一覧表示・絞り込み検索を行う画面。

#### 2.2.2 候補者一覧画面

- 登録された候補者を一覧表示する
- 以下の項目でフィルタリング・ソートができる
  - 採用種別 / 選考状況 / 担当者 / 職種 / 希望勤務地 / 大学 / 登録日時
- フリーワード検索（氏名・申送り・特記事項を対象）
- 行をクリックすると候補者詳細・編集モーダルを開く

#### 2.2.3 候補者ステータス項目

| 項目名 | 種別 | 入力方式 | 説明・制約 |
|--------|------|----------|-----------|
| 氏名 | テキスト | 手入力 | 必須 |
| 採用種別 | 選択 | ドロップダウン | 新卒 / キャリア |
| 大学 | 選択＋手入力 | マスタから選択 or 直接入力 | 大学マスタから候補表示（インクリメンタルサーチ） |
| 文理 | 選択 | ラジオボタン | 文系 / 理系 / その他 |
| 職種 | 選択＋手入力 | マスタから選択 or 直接入力 | 職種マスタから候補表示 |
| 希望勤務地 | 選択＋手入力 | マスタから選択（複数選択可） | 勤務地マスタから候補表示 |
| 担当者 | 選択 | マスタから選択 | 担当者マスタから選択 |
| 選考状況 | 選択 | ドロップダウン | 書類選考中 / 1次面接待ち / 1次面接済 / 2次面接待ち / 2次面接済 / 最終面接待ち / 最終面接済 / 内定 / 辞退 / 不採用 |
| 志望度 | 選択 | 5段階ラジオ or アイコン | ★×5段階 |
| 申送り | テキスト | 手入力（複数行） | |
| 特記事項 | テキスト | 手入力（複数行） | |
| 登録日時 | 日時 | **自動設定** | 新規登録時に自動記録 |

#### 2.2.4 候補者登録・編集

- 画面右上の「＋ 候補者追加」ボタンから登録モーダルを開く
- 一覧の行クリックで編集モーダルを開く
- 必須項目（氏名）が未入力の場合は保存を拒否し、エラーメッセージを表示する
- 削除は編集モーダル内の「削除」ボタンから実行し、確認ダイアログを表示する

---

### 2.3 面接管理機能

#### 2.3.1 概要

カレンダーUIを中心に、面接日程候補日の登録・編集・削除・候補者アサイン・面接結果登録を行う画面。

#### 2.3.2 カレンダー表示

- 月表示・週表示・日表示を切り替えられる
- 画面上部に「前へ / 今日 / 次へ」ナビゲーションを配置する
- 登録済みの面接枠はカレンダー上にブロックとして表示する（色分けで状態を視覚的に区別）

| 色 | 意味 |
|----|------|
| 青 | 空き枠（候補者未アサイン） |
| 黄 | 候補者アサイン済・結果未登録 |
| 緑 | 合格 |
| 赤 | 不合格 |
| グレー | 辞退・キャンセル |

#### 2.3.3 面接枠の登録

- **カレンダーの日付セルをクリック**または**セルをドラッグ**することで面接枠の新規登録モーダルを開く
- 日付は操作した日付が自動入力される
- 時刻は**15分間隔のドロップダウン**から選択する（手入力不可）
  - 例: 9:00 / 9:15 / 9:30 / … / 21:00
- 終了時刻は開始時刻選択後に自動候補を提示する（開始+30分、+60分、+90分など）
- 登録後、カレンダーにブロックが表示される

#### 2.3.4 面接枠の編集・移動

- カレンダー上のブロックをクリックすると編集モーダルが開く
- ブロックを**ドラッグ＆ドロップ**で別の日付・時間へ移動できる
- 移動後は自動的にデータが更新される
- ブロックの下端を**ドラッグ**して終了時刻を変更できる（リサイズ）

#### 2.3.5 面接枠の登録項目

| 項目名 | 種別 | 入力方式 | 説明 |
|--------|------|----------|------|
| 面接日 | 日付 | カレンダー操作で自動設定 / 日付ピッカーで変更可 | 必須 |
| 開始時刻 | 時刻 | 15分間隔ドロップダウン | 必須 |
| 終了時刻 | 時刻 | 15分間隔ドロップダウン | 必須 |
| 候補者 | 選択 | 候補者マスタから検索・選択（インクリメンタルサーチ） | 任意（空きの場合は空欄） |
| 面接官 | 選択 | **二段階クリック方式**（後述 §2.3.7） | 任意・複数選択可 |
| 面接形式 | 選択 | ラジオボタン | リアル / オンライン |
| 面接場所 | 選択＋手入力 | 面接場所マスタから選択 or 直接入力 | 形式がリアルの場合に表示 |
| オンラインURL | テキスト | 手入力 | 形式がオンラインの場合に表示 |
| 面接結果 | 選択 | ドロップダウン | 未実施 / 合格 / 不合格 / 辞退 / 保留 |
| 申送り | テキスト | 手入力（複数行） | |

#### 2.3.7 面接官の選択UI（二段階クリック方式）

入力者の手間を最小化しながら検索性を担保するため、面接官選択には**二段階クリック方式**を採用する。

##### 動作フロー

```
[面接官を選択] ボタンをクリック
        │
        ▼
┌──────────────────────┐
│  第1段階: 本部一覧       │  ← 面接官マスタに登録された本部が一覧表示される
│                        │
│  ● 人事本部            │
│  ● 営業本部            │  クリックすると第2段階へ →
│  ● 技術本部            │
│  ● 管理本部            │
│  …                    │
└──────────────────────┘
        │ 本部をクリック
        ▼
┌──────────────────────┬──────────────────────┐
│  第1段階: 本部一覧       │  第2段階: 面接官一覧     │  ← 選択した本部の面接官が表示
│  （選択済みをハイライト）  │                        │
│  ● 人事本部 ◀選択中     │  □ 山田 太郎（部長）    │
│  ○ 営業本部            │  □ 鈴木 花子（課長）    │  チェックで選択（複数可）
│  ○ 技術本部            │  □ 佐藤 一郎（主任）    │
│  ○ 管理本部            │  …                    │
└──────────────────────┴──────────────────────┘
        │ 面接官チェックボックスをクリック
        ▼
選択済み面接官がモーダル上部の「選択済みタグエリア」に表示される
（タグの × ボタンで個別削除可能）
```

##### UI仕様

| 要素 | 仕様 |
|------|------|
| 表示形式 | モーダル内のパネル（または面接枠登録モーダルに埋め込まれたインラインパネル） |
| 第1段階（本部列） | 面接官マスタの `本部` 値を重複排除してソート表示 |
| 第2段階（面接官列） | 選択本部に属する面接官を `氏名（役職）` 形式で表示、チェックボックスで複数選択可 |
| 本部切り替え | 別の本部をクリックすると右列が即時切り替わる（選択済みチェックは維持） |
| 選択済み表示 | パネル外（登録フォーム上）に選択面接官名をタグ形式で表示、× で個別解除可 |
| 初期状態 | 先頭の本部が自動選択され、その面接官一覧が第2段階に表示される |
| キーボード操作 | 上下矢印キーで本部・面接官を移動、スペースキーでチェック切り替え |

##### マスタ側の前提

面接官マスタの登録項目を以下のとおりとする（§2.1.1 の更新に対応）。

| 項目 | 説明 |
|------|------|
| 氏名 | 必須 |
| 本部 | 必須。二段階選択の第1段階分類に使用 |
| 部署 | 任意 |
| 役職 | 任意。第2段階の面接官一覧に併記 |

---

#### 2.3.8 面接枠の削除

- 編集モーダル内の「削除」ボタンから実行
- 削除前に確認ダイアログを表示する

---

## 3. 非機能要件

### 3.1 パフォーマンス

- 候補者1,000件・面接枠5,000件の規模でも一覧・カレンダー表示が3秒以内に完了すること

### 3.2 オフライン動作

- インターネット接続なしで全機能が利用できること
- 外部CDN（フォント・ライブラリ等）は使用しないか、または同梱すること

### 3.3 ユーザビリティ

- スクロールなしで主要操作が完結するレイアウト（1920×1080基準）
- タブキーによるフォーカス移動が正しく機能すること
- 主要なボタン・操作にツールチップを表示すること

### 3.4 データ安全性

- 全データはブラウザのIndexedDBにのみ保存し、外部送信は一切行わない
- ブラウザのキャッシュクリアやIndexedDBの削除を行うとデータが消えることをUIで警告する
- 重要操作（削除・リセット）は必ず確認ダイアログを介する

### 3.5 保守性

- HTML / CSS / JavaScript の標準技術で実装し、フレームワーク依存を最小化する
- ファイル構成はシンプルに保ち、人事担当者自身が配置・更新できる構成とする

---

## 4. 画面構成

```
┌────────────────────────────────────────────────────────────┐
│ ヘッダー（アプリ名 / ナビゲーション: 候補者 | 面接 | マスタ設定）    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  [候補者管理画面]                                           │
│  ┌─ フィルタバー（採用種別 / 選考状況 / 担当者 / 検索）─────┐  │
│  │  ┌──────────────────────────────── [＋候補者追加]┐ │  │
│  │  │ 候補者一覧テーブル                              │ │  │
│  │  │ （氏名 / 採用種別 / 大学 / 選考状況 / 担当者 …） │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────────────┘  │
│                                                            │
│  [面接管理画面]                                             │
│  ┌─ ツールバー（月/週/日 / 前へ・今日・次へ）─────────────┐  │
│  │  カレンダー                                         │  │
│  │  （面接枠ブロックをクリック・ドラッグで操作）           │  │
│  └────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 4.1 モーダル一覧

| モーダル名 | 起動トリガー | 主な操作 |
|-----------|------------|---------|
| 候補者登録モーダル | 「＋候補者追加」ボタン | 候補者情報入力・保存 |
| 候補者編集モーダル | 一覧の行クリック | 候補者情報編集・削除 |
| 面接枠登録モーダル | カレンダーセルクリック / ドラッグ | 面接枠情報入力・保存 |
| 面接枠編集モーダル | カレンダーブロッククリック | 面接枠情報編集・削除・結果登録 |
| マスタ管理モーダル | ナビ「マスタ設定」クリック | 各マスタの追加・編集・削除 |
| CSVインポートモーダル | インポートボタン | ファイル選択・インポート実行 |

---

## 5. データ設計（IndexedDB）

### 5.1 オブジェクトストア一覧

| ストア名 | 主キー | 説明 |
|---------|-------|------|
| `candidates` | `id`（自動採番） | 候補者情報 |
| `interviews` | `id`（自動採番） | 面接枠情報 |
| `masters_interviewers` | `id` | 面接官マスタ |
| `masters_universities` | `id` | 大学マスタ |
| `masters_jobTypes` | `id` | 職種マスタ |
| `masters_locations` | `id` | 勤務地マスタ |
| `masters_hrStaff` | `id` | 担当者マスタ |
| `masters_rooms` | `id` | 面接場所マスタ |

### 5.2 candidatesスキーマ

```json
{
  "id": "number (auto)",
  "name": "string",
  "recruitType": "'新卒' | 'キャリア'",
  "university": "string",
  "facultyType": "'文系' | '理系' | 'その他'",
  "jobType": "string",
  "preferredLocations": "string[]",
  "hrStaff": "string",
  "selectionStatus": "string",
  "motivation": "number (1-5)",
  "notes": "string",
  "remarks": "string",
  "createdAt": "ISO8601 string"
}
```

### 5.3 masters_interviewersスキーマ

```json
{
  "id": "number (auto)",
  "name": "string",
  "division": "string",
  "department": "string",
  "title": "string"
}
```

- `division`（本部）: 二段階クリック選択の第1段階分類キー。必須。
- `department`（部署）: 任意。
- `title`（役職）: 任意。第2段階の面接官一覧に `氏名（役職）` 形式で表示。

### 5.4 interviewsスキーマ

```json
{
  "id": "number (auto)",
  "date": "YYYY-MM-DD",
  "startTime": "HH:MM",
  "endTime": "HH:MM",
  "candidateId": "number | null",
  "interviewerIds": "number[]",
  "format": "'リアル' | 'オンライン'",
  "location": "string",
  "onlineUrl": "string",
  "result": "'未実施' | '合格' | '不合格' | '辞退' | '保留'",
  "notes": "string",
  "createdAt": "ISO8601 string",
  "updatedAt": "ISO8601 string"
}
```

---

## 6. CSVフォーマット

### 6.1 候補者CSVエクスポート列順

```
id, 氏名, 採用種別, 大学, 文理, 職種, 希望勤務地, 担当者, 選考状況, 志望度, 申送り, 特記事項, 登録日時
```

### 6.2 候補者CSVインポート

- 1行目はヘッダー行（上記と同じ列名）
- `id` 列は無視する（インポート時に新規採番）
- 登録日時が空の場合はインポート実行日時を設定する

### 6.3 面接CSVエクスポート列順

```
id, 面接日, 開始時刻, 終了時刻, 候補者名, 面接官, 面接形式, 面接場所, オンラインURL, 面接結果, 申送り, 登録日時, 更新日時
```

---

## 7. 実装技術方針

| 項目 | 方針 |
|------|------|
| 言語 | HTML5 / CSS3 / Vanilla JavaScript（ES2020+） |
| 外部ライブラリ | 最小限（完全オフライン動作のためすべて同梱） |
| カレンダーUI | FullCalendar（ローカルバンドル）またはゼロ依存の自作実装 |
| IndexedDB操作 | idb ライブラリ（ローカルバンドル）またはネイティブIDBAPIのラッパー |
| ドラッグ＆ドロップ | FullCalendarの組み込み機能、またはHTML5 Drag and Drop API |
| スタイル | CSSカスタムプロパティでテーマ管理 |
| ファイル構成 | `index.html` + `css/` + `js/` + `lib/`（同梱ライブラリ） |

---

## 8. 優先度・フェーズ計画

| フェーズ | 内容 | 優先度 |
|---------|------|-------|
| Phase 1 | 候補者管理（一覧・登録・編集・削除・CSV） | 最高 |
| Phase 2 | マスタ管理（面接官・大学・職種・担当者等） | 高 |
| Phase 3 | 面接カレンダー（月/週/日表示・枠登録・編集） | 高 |
| Phase 4 | 面接枠へのドラッグ移動・リサイズ | 中 |
| Phase 5 | 面接結果登録・カレンダー色分け | 中 |
| Phase 6 | 面接情報CSVエクスポート | 中 |
| Phase 7 | パフォーマンス最適化・UX改善 | 低 |

---

## 9. 用語集

| 用語 | 説明 |
|------|------|
| 候補者 | 採用選考中の応募者 |
| 面接枠 | カレンダー上に登録された面接の日時スロット |
| マスタ | 事前登録情報（面接官・大学など）。フォーム上でワンクリック選択できる |
| 選考状況 | 候補者が現在どの選考ステップにいるかを示すステータス |
| IndexedDB | ブラウザ内蔵のクライアントサイドデータベース |

---

*以上*
